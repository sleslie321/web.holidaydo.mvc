# Copilot Instructions

## Project Guidelines
- For destination breadcrumbs in this codebase, the first breadcrumb item is a region and should link to /region/{slug}/{destinationId}; later breadcrumb items should use /destination/{slug}/{destinationId}.
- Destination breadcrumbs should use a compact chevron-style visual matching the provided reference image, with a home icon first and subtle gray borders/background.